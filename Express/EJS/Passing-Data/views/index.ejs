<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Locals</title>
</head>
<body>
    <% if (locals.data1) { %>
        <h1><%=data1.name%></h1>
        <h1><%=data1.email%></h1>
    <% } else { %> <h1>Does not exist</h1> <% } %>
    
    <% if (locals.data2) { %>
        <h1><%=data2.name%></h1>
        <h1><%=data2.email%></h1>
    <% } else { %> <h1>Does not exist</h1> <% } %>

</body>
</html>